class TiendaDM {
    fun buscaProducto() {
        // Segun los parametros de busqueda que introduzca el usuario, buscaremos entre los objetos Productos de la lista que tenemos
    }
    fun buscaDisco() {
        // Segun los parametros de busqueda que introduzca el usuario, buscaremos entre los objetos Disco de la lista que tenemos
    }
    fun buscapelicula(){
        // Segun los parametros de busqueda que introduzca el usuario, buscaremos entre los objetos Pelicula de la lista que tenemos
    }
    fun compraPelicula(){
        // Llamamos al metodo pagar de Cliente
    }
    fun compraDisco(){
        // Llamamos al metodo pagar de Cliente, y actualizaremos el stock de la clase Disco
    }
}